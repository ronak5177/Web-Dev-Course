import React from "react";

const page = () => {
  return (
    <div>
      <h2 className="font-bold">This is about-2 page</h2>
      <p>
        where we use rewrite it on url it will show /about but in the real time
        it will send a data for about-2 page
      </p>
    </div>
  );
};

export default page;
