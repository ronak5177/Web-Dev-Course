import React from 'react'

const about = () => {
  return (
    <div>
      this is about page
    </div>
  )
}

export default about

export const metadata = {
    title: "About Facebook App",
    description: "This is About page of facebook, we can connect with world using facebook",
  };